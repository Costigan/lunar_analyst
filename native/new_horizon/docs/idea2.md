We're debugging subtle differences between the slope generated by the reference algorithm and the QuadTree algorithm when the sampled terrain points are very close to the observer.
Let's consider a different approach.
Let's say you modify the QuadTree algorithm's minimum distance to be 50 meters.  It then continues normally and produces an horizon.
It can then (or in parallel) use the exact method the reference algorithm uses but limiting the raycasting to 50 meters along the straight cartesian vector and calculates a horizon.
It then merges these two horizons by maxing them.
This completely sidesteps all efforts to replicate the exact calculation with the approximations when very close to the observer.
What do you think?
