# MoonLayers Sync Policy

## Authoritative Development Location
- Active MoonLayers development happens in this repository at `moonlayers_pkg/`.

## External Repository Status
- `D:\projects\moonlayers` remains available as a legacy mirror/reference.
- Do not treat `D:\projects\moonlayers` as the primary edit target for day-to-day Lunar Analyst work.

## Initial Import Snapshot
- Imported into `lunar_analyst/moonlayers_pkg` from external repo commit:
  - `8b88ab41db5c5e4bc0872474b4c8a79dc144d177`

## Sync Direction
- Default direction is one-way from `lunar_analyst/moonlayers_pkg` to any external mirror when/if a manual sync is explicitly requested.
- Avoid bidirectional ad-hoc edits to prevent divergence and ambiguous import/runtime behavior.
